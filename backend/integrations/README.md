# Integrations

Provider-specific WordPress, Google Business Profile, Cloudinary, SMTP, and AI adapters will move here incrementally. Existing `api/_lib` adapters remain active until each migration is tested.
