export class ContentRefreshEngine { analyze(documents, performances, staleDays = 180) { const byContent = new Map(performances.map((item) => [String(item.contentId), item])); return documents.filter((item) => item.status === "published").map((item) => { const performance = byContent.get(String(item._id)), ageDays = Math.floor((Date.now() - new Date(item.publishedAt || item.updatedAt).getTime()) / 86400000), rankingDrop = Number(performance?.previousRanking || performance?.ranking || 0) - Number(performance?.ranking || 0), trafficDrop = performance?.previousTraffic ? Math.round((performance.previousTraffic - performance.traffic) / performance.previousTraffic * 100) : 0, reasons = []; if (ageDays >= staleDays) reasons.push("Content is stale"); if (rankingDrop >= 3) reasons.push(`Ranking dropped ${rankingDrop} positions`); if (trafficDrop >= 20) reasons.push(`Traffic dropped ${trafficDrop}%`); return reasons.length ? { contentId: item._id, title: item.title, reasons, priority: rankingDrop >= 5 || trafficDrop >= 35 ? "High" : "Medium", actions: ["Update dated facts and statistics", "Add missing sections and FAQs", "Review title and search intent", "Improve contextual internal links"] } : null; }).filter(Boolean); } }
