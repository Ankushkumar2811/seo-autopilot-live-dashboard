import { BaseAgent } from "../BaseAgent.js"; import { ContentFactoryService } from "../../services/content-factory/ContentFactoryService.js"; import { estimateMetrics } from "../agent-utils.js"; export class ContentBriefAgent extends BaseAgent { constructor(deps) { super({ id: "content-brief", name: "Content Brief Agent", description: "Creates production-ready SEO briefs from prioritized clusters", ...deps }); } async execute(input, context) { const service = new ContentFactoryService({ db: context.db }), project = await service.project(input.projectId, context), briefs = await service.createBriefs(project, context, input.limit); return { output: { count: briefs.length, briefs }, metrics: estimateMetrics(briefs) }; } }
