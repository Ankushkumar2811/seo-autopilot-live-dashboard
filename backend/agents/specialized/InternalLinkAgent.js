import { BaseAgent } from "../BaseAgent.js"; import { ContentFactoryService } from "../../services/content-factory/ContentFactoryService.js"; import { estimateMetrics } from "../agent-utils.js"; export class InternalLinkAgent extends BaseAgent { constructor(deps) { super({ id: "content-internal-link", name: "Content Internal Link Agent", description: "Builds evidence-based source, target, anchor and reason suggestions", ...deps }); } async execute(input, context) { const service = new ContentFactoryService({ db: context.db }), project = await service.project(input.projectId, context), suggestions = await service.linking(project, context); return { output: { suggestions }, metrics: estimateMetrics(suggestions) }; } }
