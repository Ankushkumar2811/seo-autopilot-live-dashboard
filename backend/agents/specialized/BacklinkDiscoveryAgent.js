import { BaseAgent } from "../BaseAgent.js"; import { AuthorityService } from "../../services/authority/AuthorityService.js"; import { estimateMetrics } from "../agent-utils.js"; export class BacklinkDiscoveryAgent extends BaseAgent { constructor(deps) { super({ id: "backlink-discovery", name: "Backlink Discovery Agent", description: "Finds scored directory, partner, editorial, association, resource and media opportunities", ...deps }); } async execute(input, context) { const service = new AuthorityService({ db: context.db }), project = await service.project(input.projectId, context), opportunities = await service.discover(project, context); await this.events.emit("backlink_found", { projectId: project._id, count: opportunities.length }, context); return { output: { opportunities }, metrics: estimateMetrics(opportunities) }; } }
