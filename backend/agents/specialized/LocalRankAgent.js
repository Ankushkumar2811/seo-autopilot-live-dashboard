import { BaseAgent } from "../BaseAgent.js"; import { LocalSeoService } from "../../services/local-seo/LocalSeoService.js"; import { estimateMetrics } from "../agent-utils.js"; export class LocalRankAgent extends BaseAgent { constructor(deps) { super({ id: "local-rank", name: "Local Rank Agent", description: "Persists local-pack and organic observations with movement history", ...deps }); } async execute(input, context) { const service = new LocalSeoService({ db: context.db }), project = await service.project(input.projectId, context), output = await service.trackRanks(project, input, context); return { output, metrics: estimateMetrics(output, input.provider || "internal") }; } }
