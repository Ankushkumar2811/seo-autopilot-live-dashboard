import { BaseAgent } from "../BaseAgent.js"; import { ContentFactoryService } from "../../services/content-factory/ContentFactoryService.js"; import { estimateMetrics } from "../agent-utils.js"; export class ContentLearningAgent extends BaseAgent { constructor(deps) { super({ id: "content-learning", name: "Content Learning Agent", description: "Learns from imported performance data without inventing analytics", ...deps }); } async execute(input, context) { const service = new ContentFactoryService({ db: context.db }), project = await service.project(input.projectId, context), output = await service.learn(project, context); return { output, metrics: estimateMetrics(output) }; } }
