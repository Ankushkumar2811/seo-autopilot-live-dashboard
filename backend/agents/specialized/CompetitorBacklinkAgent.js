import { BaseAgent } from "../BaseAgent.js"; import { AuthorityService } from "../../services/authority/AuthorityService.js"; import { estimateMetrics } from "../agent-utils.js"; export class CompetitorBacklinkAgent extends BaseAgent { constructor(deps) { super({ id: "competitor-backlink", name: "Competitor Backlink Agent", description: "Turns provider observations into common-domain and authority-gap reports", ...deps }); } async execute(input, context) { const service = new AuthorityService({ db: context.db }), project = await service.project(input.projectId, context), output = await service.competitorGap(project, input, context); return { output, metrics: estimateMetrics(output, input.provider || "internal") }; } }
