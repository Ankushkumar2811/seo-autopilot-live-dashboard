import { BaseAgent } from "../BaseAgent.js"; import { ContentFactoryService } from "../../services/content-factory/ContentFactoryService.js"; import { estimateMetrics } from "../agent-utils.js"; export class ContentCalendarAgent extends BaseAgent { constructor(deps) { super({ id: "content-calendar", name: "Content Calendar Agent", description: "Creates priority and seasonality-aware publishing calendars", ...deps }); } async execute(input, context) { const service = new ContentFactoryService({ db: context.db }), project = await service.project(input.projectId, context), calendar = await service.buildCalendar(project, context, input); return { output: { calendar }, metrics: estimateMetrics(calendar) }; } }
