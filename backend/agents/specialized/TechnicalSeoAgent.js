import{BaseAgent}from"../BaseAgent.js";import{inspectWebsite,estimateMetrics}from"../agent-utils.js";
export class TechnicalSeoAgent extends BaseAgent{constructor(deps){super({id:"technical-seo",name:"Technical SEO Agent",description:"Builds prioritized technical SEO fix recommendations",...deps});}async execute(input){const audit=input.audit||await inspectWebsite(input.url);const fixes=audit.issues.map(issue=>({issue:issue.type,severity:issue.severity,recommendation:({missing_title:"Add a unique 45-60 character title aligned with search intent.",missing_description:"Add a compelling 120-155 character meta description.",h1_count:"Keep exactly one descriptive H1.",image_alt:"Add concise descriptive alt text to meaningful images.",canonical:"Add a self-referencing canonical URL.",schema:"Add validated JSON-LD matching the page type."}[issue.type]||issue.message),autoFixSafe:false}));const checks={redirects:"Run a crawl before changing redirects",brokenLinks:`Review ${audit.links?.length||0} discovered links`,robots:audit.hasRobots?"present":"verify robots.txt and add page directives where needed",sitemap:"Verify /sitemap.xml is reachable"};const output={score:audit.score,fixes,checks};return{output,metrics:estimateMetrics(output)};}}
