import { BaseAgent } from "../BaseAgent.js"; import { LocalSeoService } from "../../services/local-seo/LocalSeoService.js"; import { estimateMetrics } from "../agent-utils.js"; export class LocalCompetitorAgent extends BaseAgent { constructor(deps) { super({ id: "local-competitor", name: "Local Competitor Agent", description: "Compares declared competitor GBP metrics and explains advantages", ...deps }); } async execute(input, context) { const service = new LocalSeoService({ db: context.db }), project = await service.project(input.projectId, context), output = await service.competitors(project, input, context); return { output, metrics: estimateMetrics(output) }; } }
