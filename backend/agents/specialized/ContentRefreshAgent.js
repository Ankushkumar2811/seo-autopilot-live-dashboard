import { BaseAgent } from "../BaseAgent.js"; import { ContentFactoryService } from "../../services/content-factory/ContentFactoryService.js"; import { estimateMetrics } from "../agent-utils.js"; export class ContentRefreshAgent extends BaseAgent { constructor(deps) { super({ id: "content-refresh", name: "Content Refresh Agent", description: "Finds stale or declining content and proposes refresh actions", ...deps }); } async execute(input, context) { const service = new ContentFactoryService({ db: context.db }), project = await service.project(input.projectId, context), recommendations = await service.refreshAnalysis(project, context); return { output: { recommendations }, metrics: estimateMetrics(recommendations) }; } }
