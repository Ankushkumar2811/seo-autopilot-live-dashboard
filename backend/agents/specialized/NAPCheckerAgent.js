import { BaseAgent } from "../BaseAgent.js"; import { LocalSeoService } from "../../services/local-seo/LocalSeoService.js"; import { estimateMetrics } from "../agent-utils.js"; export class NAPCheckerAgent extends BaseAgent { constructor(deps) { super({ id: "nap-checker", name: "NAP Checker Agent", description: "Safely scans supplied public listing URLs for name, address, phone and website mismatches", ...deps }); } async execute(input, context) { const service = new LocalSeoService({ db: context.db }), project = await service.project(input.projectId, context), output = await service.checkNap(project, input, context); return { output, metrics: estimateMetrics(output) }; } }
