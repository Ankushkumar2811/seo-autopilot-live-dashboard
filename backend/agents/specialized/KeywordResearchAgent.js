import { BaseAgent } from "../BaseAgent.js"; import { ContentFactoryService } from "../../services/content-factory/ContentFactoryService.js"; import { estimateMetrics } from "../agent-utils.js"; export class KeywordResearchAgent extends BaseAgent { constructor(deps) { super({ id: "keyword-research", name: "Keyword Research Agent", description: "Creates intent, priority, business-value and target-page keyword maps", ...deps }); } async execute(input, context) { const service = new ContentFactoryService({ db: context.db }), project = await service.project(input.projectId, context), output = await service.buildAuthority(project, context); return { output: { keywordMap: output.clusters }, metrics: estimateMetrics(output) }; } }
