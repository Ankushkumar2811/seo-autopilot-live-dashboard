import { BaseAgent } from "../BaseAgent.js"; import { AuthorityService } from "../../services/authority/AuthorityService.js"; import { estimateMetrics } from "../agent-utils.js"; export class DigitalPRAgent extends BaseAgent { constructor(deps) { super({ id: "digital-pr", name: "Digital PR Agent", description: "Creates research, commentary, interview, podcast and webinar PR concepts", ...deps }); } async execute(input, context) { const service = new AuthorityService({ db: context.db }), project = await service.project(input.projectId, context), opportunities = await service.pr(project, context); return { output: { opportunities }, metrics: estimateMetrics(opportunities) }; } }
