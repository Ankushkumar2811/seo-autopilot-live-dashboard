import { BaseAgent } from "../BaseAgent.js"; import { AuthorityService } from "../../services/authority/AuthorityService.js"; import { estimateMetrics } from "../agent-utils.js"; export class AuthorityReportAgent extends BaseAgent { constructor(deps) { super({ id: "authority-report", name: "Authority Report Agent", description: "Connects backlink, PR, GEO and content signals into an authority growth report", ...deps }); } async execute(input, context) { const service = new AuthorityService({ db: context.db }), project = await service.project(input.projectId, context), report = await service.report(project, context); return { output: report, metrics: estimateMetrics(report) }; } }
