import { BaseAgent } from "../BaseAgent.js"; import { LocalSeoService } from "../../services/local-seo/LocalSeoService.js"; import { estimateMetrics } from "../agent-utils.js"; export class LocalSeoReportAgent extends BaseAgent { constructor(deps) { super({ id: "local-seo-report", name: "Local SEO Report Agent", description: "Generates client local ranking, review, GBP, citation and next-action reports", ...deps }); } async execute(input, context) { const service = new LocalSeoService({ db: context.db }), project = await service.project(input.projectId, context), report = await service.scoreAndReport(project, context); return { output: report, metrics: estimateMetrics(report) }; } }
