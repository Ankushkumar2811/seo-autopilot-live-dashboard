import { BaseAgent } from "../BaseAgent.js"; import { AuthorityService } from "../../services/authority/AuthorityService.js"; import { estimateMetrics } from "../agent-utils.js"; export class BrandMentionAgent extends BaseAgent { constructor(deps) { super({ id: "brand-mention", name: "Brand Mention Agent", description: "Scans supplied public news, blog, forum and social URLs for unlinked mentions", ...deps }); } async execute(input, context) { const service = new AuthorityService({ db: context.db }), project = await service.project(input.projectId, context), output = await service.scanMentions(project, input, context); return { output, metrics: estimateMetrics(output) }; } }
