import { BaseAgent } from "../BaseAgent.js"; import { ContentFactoryService } from "../../services/content-factory/ContentFactoryService.js"; import { estimateMetrics } from "../agent-utils.js"; export class TopicalAuthorityAgent extends BaseAgent { constructor(deps) { super({ id: "topical-authority", name: "Topical Authority Agent", description: "Builds pillar and supporting topic clusters with authority gaps", ...deps }); } async execute(input, context) { const service = new ContentFactoryService({ db: context.db }), project = await service.project(input.projectId, context), output = await service.buildAuthority(project, context); return { output, metrics: estimateMetrics(output) }; } }
