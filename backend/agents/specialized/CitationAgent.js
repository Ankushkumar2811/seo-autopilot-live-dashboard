import { BaseAgent } from "../BaseAgent.js"; import { LocalSeoService } from "../../services/local-seo/LocalSeoService.js"; import { estimateMetrics } from "../agent-utils.js"; export class CitationAgent extends BaseAgent { constructor(deps) { super({ id: "citation-management", name: "Citation Management Agent", description: "Creates and tracks local, industry, chamber, association and partner opportunities", ...deps }); } async execute(input, context) { const service = new LocalSeoService({ db: context.db }), project = await service.project(input.projectId, context), citations = await service.citations(project, context); return { output: { citations }, metrics: estimateMetrics(citations) }; } }
