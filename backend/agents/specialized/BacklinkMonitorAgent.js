import { BaseAgent } from "../BaseAgent.js"; import { AuthorityService } from "../../services/authority/AuthorityService.js"; import { estimateMetrics } from "../agent-utils.js"; export class BacklinkMonitorAgent extends BaseAgent { constructor(deps) { super({ id: "backlink-monitor", name: "Backlink Monitor Agent", description: "Verifies public backlink evidence and detects lost, broken, anchor and nofollow changes", ...deps }); } async execute(input, context) { const service = new AuthorityService({ db: context.db }), project = await service.project(input.projectId, context), output = await service.monitorLinks(project, context); return { output, metrics: estimateMetrics(output) }; } }
