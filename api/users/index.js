import { ObjectId } from "mongodb"; import { withApiHandler } from "../../backend/middleware/api-handler.js"; import { requireMethod,sendJson } from "../_lib/http.js"; import { Permissions } from "../../backend/security/permissions.js"; import { requireDb } from "../auth/_shared.js";
async function handler(req,res){if(!requireMethod(req,res,["GET"]))return;const db=await requireDb();const organizationId=new ObjectId(req.context.identity.organizationId);const users=await db.collection("users").find({organizationId},{projection:{passwordHash:0}}).toArray();sendJson(res,200,{ok:true,users:users.map(u=>({...u,id:u._id.toString(),_id:undefined,organizationId:u.organizationId.toString()}))});} export default withApiHandler(handler,{authRequired:true,permission:Permissions.TEAM_MANAGE});
