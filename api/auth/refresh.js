import { withApiHandler } from "../../backend/middleware/api-handler.js"; import { requireMethod,sendJson } from "../_lib/http.js"; import { rotateSession } from "../../backend/services/auth-service.js"; import { getRefreshToken,setAuthCookies } from "../../backend/security/cookies.js"; import { requireDb } from "./_shared.js";
async function handler(req,res){if(!requireMethod(req,res,["POST"]))return;const session=await rotateSession(await requireDb(),getRefreshToken(req));setAuthCookies(res,session.accessToken,session.refreshToken);sendJson(res,200,{ok:true,user:session.user,expiresIn:session.expiresIn});} export default withApiHandler(handler,{authRequired:false});
