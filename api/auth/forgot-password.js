import { withApiHandler } from "../../backend/middleware/api-handler.js"; import { readJson,requireMethod,sendJson } from "../_lib/http.js"; import { requestPasswordReset } from "../../backend/services/auth-service.js"; import { enforceRateLimit } from "../../backend/services/rate-limit-service.js"; import { requireDb } from "./_shared.js";
async function handler(req,res){if(!requireMethod(req,res,["POST"]))return;const db=await requireDb();await enforceRateLimit(db,req,"forgot",{limit:5,windowSeconds:3600});const body=await readJson(req);await requestPasswordReset(db,body.email);sendJson(res,200,{ok:true,message:"If the account exists, a reset email has been sent."});} export default withApiHandler(handler,{authRequired:false});
